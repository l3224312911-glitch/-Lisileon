“符号链接”（Symbolic Link） 或 “目录联接”（Directory Junction）就像是在 C 盘放了一个“传送门”，文件看似还在 C 盘，但实际存储在其他磁盘（如 D 盘）。

具体操作步骤如下：

第一步：准备工作（非常重要）

找到要搬家的文件夹：假设要搬移 C:\Games 这个文件夹。

移动文件夹：

先将 C:\Games 整个文件夹剪切并粘贴到 D 盘（或其他空间大的盘），例如 D:\Games。

注意：此时 C 盘里不应该再有名为 Games 的文件夹了。

第二步：使用 CMD 创建链接

点击任务栏的“搜索”，输入 CMD。

在搜索结果中，右键点击“命令提示符”，选择 “以管理员身份运行”（必须是管理员权限）。

在黑色窗口中输入以下指令（请根据你的实际路径修改）：

mklink /j "C盘原路径" "D盘新路径"
举个例子：

如果你把 C:\ABC 挪到了 D:\ABC，那么指令就是：
mklink /j "C:\ABC" "D:\ABC"

按回车键后，如果显示：
为 C:\ABC <<===>> D:\ABC 创建了联接
这就说明成功了！

指令详解与注意事项

/j 参数：代表“目录联接”（Junction）。这是最推荐用于文件夹搬家的方式，它在系统底层会被识别为本地文件夹，兼容性最好。

引号：如果你的文件夹路径中包含空格（比如 Program Files），请务必用英文双引号 "" 把路径包起来。

C 盘不能有同名文件夹：执行命令前，C 盘的那个原位置必须是空的（或者已经删除了旧文件夹）。如果 C 盘已经存在该文件夹，命令会报错“当文件已存在时，无法创建该文件”。

图标标识：成功后，你会发现 C 盘出现了一个带有小箭头（类似快捷方式）的文件夹，但它的属性显示依然占用空间（实际上是虚报，不占用 C 盘物理空间）。

常见应用场景

iTunes 备份文件夹：C:\Users\你的用户名\Apple\MobileSync\Backup（这个非常占空间，适合迁走）。

微信/QQ 聊天记录：如果软件设置里改不了，可以用这招。

游戏安装包：某些强制装在 C 盘的软件或游戏。

如何还原？

如果你想取消这个链接，直接删除 C 盘那个带小箭头的文件夹即可。这只会删除“链接关系”，不会删除 D 盘里的实际文件。
